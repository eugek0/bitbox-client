import { FC } from "react";
import LogsSettings from "@/components/Settings/LogsSettings";

const LogsSettingsContainer: FC = () => {
  return <LogsSettings />;
};

export default LogsSettingsContainer;
